#!/bin/bash

echo "🚀 Moving admin pages to /admin subfolder..."

# Create admin directories
mkdir -p app/admin/create
mkdir -p app/admin/demo
mkdir -p app/admin/record

# Move files
if [ -f "app/create/page.tsx" ]; then
    mv app/create/page.tsx app/admin/create/page.tsx
    rmdir app/create 2>/dev/null || true
    echo "✅ Moved create page"
fi

if [ -f "app/demo/page.tsx" ]; then
    mv app/demo/page.tsx app/admin/demo/page.tsx
    rmdir app/demo 2>/dev/null || true
    echo "✅ Moved demo page"
fi

if [ -f "app/record/page.tsx" ]; then
    mv app/record/page.tsx app/admin/record/page.tsx
    rmdir app/record 2>/dev/null || true
    echo "✅ Moved record page"
fi

echo "🎉 Done! Admin pages moved to /admin subfolder"
